export const VM = {
  REGISTERS: {
    A: '',
    X: '',
    Y: '',
    S: '',
    PC: '',
  },
  MEMORY: ''
}

// Loads a byte of memory into the accumulator
// setting the zero and negative flags as appropriate
export function LDA (asm) {
}
